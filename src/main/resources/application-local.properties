# Local development configuration
# This overrides the main application.properties for local development

# Use the old Gmail SMTP configuration for local development
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anthonypate@gmail.com
spring.mail.password=cyti tmxn neqh ukkf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.test-connection=false

# Email provider selection (smtp or ses)
email.provider=smtp

# Optional: Redirect all emails to a test email for safer local testing
# Uncomment and set your test email to redirect all outgoing emails
# email.testing.redirect=your-test-email@gmail.com

# Local development base URL
app.base-url=http://localhost:8080

# Local database settings (if needed)
spring.datasource.url=jdbc:postgresql://localhost:5432/familynest_test
spring.datasource.username=postgres
spring.datasource.password=password

# Subscription pricing - should be managed by admin interface in production
subscription.monthly.price=2.99
